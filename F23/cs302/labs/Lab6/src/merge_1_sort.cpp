// Will Buziak
// CS302
// Lab 6
// Merge Sort

#include <iostream>
#include "Sorting.hpp"
using namespace std;

vector <double> merge(vector <double> &v) {
  

  if (v.size() == 1) return v;
  if (v.size() == 2) {
    // sort with an if statement, maybe swap
  }
}

int main() {

  return 1;
}
